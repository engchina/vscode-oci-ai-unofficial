export function openViewLabel(name: string) {
  return `Open ${name}`
}

export function openWorkspaceLabel(name: string) {
  return `Open ${name} Workspace`
}

export function backToLabel(name: string) {
  return `Back to ${name}`
}

export function showInListLabel(name: string) {
  return `Show ${name} in List`
}
